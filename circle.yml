dependencies:
  pre:
    - sudo apt-get install less

deployment:
  production:
    branch: master
    commands:
      - git remote add dokku dokku@ga.barista-v.com:master
      - git push dokku master
  staging:
    branch: staging
    commands:
      - git remote add dokku dokku@ga.barista-v.com:staging
      - git push dokku staging:master
